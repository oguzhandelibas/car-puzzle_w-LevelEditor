using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace CarLotJam
{
    [CreateAssetMenu(fileName = "StickmanData", menuName = "ScriptableObjects/CarModule/CarData", order = 1)]
    public class StickmanData : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
